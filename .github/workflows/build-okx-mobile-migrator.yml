# Build completed. This workflow is intentionally disabled after producing the mobile migration APK.
name: OKX Mobile Migrator Build (disabled)
on:
  workflow_dispatch:
jobs:
  noop:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo disabled
